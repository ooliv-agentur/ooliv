
import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

// Export the component directly for better compatibility
// with the background patterns and video layers
const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
